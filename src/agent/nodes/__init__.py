"""Shared pure helpers surviving from the pipeline era.

The selection loop's tools (``agents/tools.py``) reuse the hot-filter rules
and the score-sanitize contract from here; the LangGraph nodes themselves
were removed when the graph runtime was retired.
"""
